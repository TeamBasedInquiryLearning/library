<?xml version='1.0' encoding='utf-8'?>

<section xml:id="AI4" xmlns:xi="http://www.w3.org/2001/XInclude" tbil-slides="build">
  <title>Surface Areas of Revolution (AI4)</title>
    <objectives>
        <ul>
            <li>
                <xi:include href="outcomes/04.ptx"/>
            </li>
        </ul>
    </objectives>
  <subsection>
    <title>Activities</title>
    
    
    
    <fact>
        <statement>
        <p>
            A <term>frustum</term><idx>frustum</idx> is the portion of a cone that lies between one or two parallel planes.
            
            <figure>
                <caption>Plot of a frustum.</caption>
                    <image xml:id="frustum" width="50%">
                        <description>frustum</description>
                        <sageplot>
                            from sage.plot.graphics import Graphics
                            geometry=Graphics()
                            x = var('x')
                            f(x) = 0.5*x
                            g(x) = -0.5*x
                            p = plot([f,g],(x,0,4),thickness=2,color='blue', ticks=[[],[]])
                            p_shade = plot(f,(x,2,3),fill=g)
                            small_r = line(([2,f(2)],[2,0]),thickness=3,color='red')
                            small_r_label = text('$r$',(1.9,0.5*f(1.9)), color='black',fontsize=13)
                            big_r = line(([3,f(3)],[3,0]),thickness=3,color='red')
                            big_r_label = text('$R$',(3.1,0.5*f(3.1)), color='black',fontsize=13)
                            length = line(([2,f(2)],[3,f(3)]),thickness=3,color='lightgreen')
                            length_label= text('$l$',(2.5,f(2.5)+.2),color='black',fontsize=13)
                            geometry += p
                            geometry += p_shade
                            geometry += small_r
                            geometry += big_r
                            geometry += small_r_label
                            geometry += big_r_label
                            geometry += length
                            geometry += length_label
                            geometry
                        </sageplot>
                    </image>
            </figure>
            </p>
            
            <p>The surface area of the <q>side</q> of the frustum is: <me>2\pi \frac{r+R}{2}\cdot l</me> where <m>r</m> and <m>R</m> are the radii of the bases, and <m>l</m> is the length of the side.</p>
        
        <p>Note that if <m>r=R</m>, this reduces to the surface area of a <q>side</q> of a cylinder.</p>
            </statement>
    </fact>
    
    <activity>
        <introduction>
            <p>
                Suppose we wanted to find the surface area of the the solid of revolution generated by rotating
                    <me>y=\sqrt{x}, 0\leq x\leq 4</me>
                    about the <m>y</m>-axis.
                
                <figure>
                <caption>Plot of bounded region rotated about <m>x</m>-axis.</caption>
                    <image xml:id="rotatingrootxaboutx" width="50%">
                        <description>Bounded region rotated about <m>x</m>-axis.</description>
                        <sageplot>
                            from sage.plot.graphics import Graphics
                            geometry=Graphics()
                            x = var('x')
                            f(x) = sqrt(x)
                            g(x) = -1*f(x)
                            p1 = plot(f,(x,0,4),thickness=2,fill=g, fillalpha=0.25,ticks=[1,1])
                            p2 = plot(g,(x,0,4),thickness=2,linestyle='dashed')
                            geometry += p1
                            geometry += p2
                            geometry
                        </sageplot> 
                    </image>
            </figure>
            </p>
        </introduction>
        
        <task>
            <p>
                Suppose we wanted to estimate the surface area with two frustums with <m>\Delta x=2</m>.
                
                <figure>
                <caption>Plot of bounded region rotated about <m>x</m>-axis.</caption>
                    <image xml:id="rotatingrootxaboutxtwopiece" width="50%">
                        <description>Bounded region rotated about <m>x</m>-axis.</description>
                        <sageplot>
                            from sage.plot.graphics import Graphics
                            geometry=Graphics()
                            x = var('x')
                            f(x) = sqrt(x)
                            g(x) = -1*f(x)
                            p1 = plot(f,(x,0,4),thickness=2,fill=g, fillalpha=0.25,ticks=[1,0.5])
                            p2 = plot(g,(x,0,4),thickness=2,linestyle='dashed')
                            f0 = circle((0,0),0.05,fill=True,facecolor='black')
                            f2 = circle((2,f(2)),0.05,fill=True,facecolor='black')
                            f4 = circle((4,f(4)),0.05,fill=True,facecolor='black')
                            line0 = line(([0,0],[2,f(2)]),linestyle='dashed',color='black')
                            line2 = line(([2,f(2)],[4,f(4)]),linestyle='dashed',color='black')
                            geometry += p1
                            geometry += p2
                            geometry += f0
                            geometry += f2
                            geometry += f4
                            geometry += line0
                            geometry += line2
                            geometry
                        </sageplot>
                    </image>
            </figure>
                What is the  surface area of the frustum formed by rotating the line segment from <m>(0,0)</m> to <m>(2, \sqrt{2})</m> about the <m>x</m>-axis?
                
                <ol marker='A'>
                    <li><m>2\pi \frac{0+\sqrt{2}}{2}\cdot2</m></li>
                    <li><m>2\pi \frac{0+\sqrt{2}}{2}\cdot\sqrt{2^2+\sqrt{2}^2}</m></li>
                    <li><m>\pi \sqrt{2}^2\cdot2</m></li>
                    <li><m>\pi \sqrt{2}^2\cdot\sqrt{2^2+\sqrt{2}^2}</m></li>
                </ol>
            </p>
        </task>
        
        <task>
            <p>
                <figure>
                <caption>Plot of bounded region rotated about the <m>x</m>-axis.</caption>
                    <image xml:id="rotatingrootxaboutxtwopiece2" width="50%">
                        <description>Bounded region rotated about <m>x</m>-axis.</description>
                        <sageplot>
                            from sage.plot.graphics import Graphics
                            geometry=Graphics()
                            x = var('x')
                            f(x) = sqrt(x)
                            g(x) = -1*f(x)
                            p1 = plot(f,(x,0,4),thickness=2,ticks=[1,0.5])
                            p2 = plot(g,(x,0,4),thickness=2,linestyle='dashed')
                            f0 = circle((0,0),0.05,fill=True,facecolor='black')
                            f2 = circle((2,f(2)),0.05,fill=True,facecolor='black')
                            f4 = circle((4,f(4)),0.05,fill=True,facecolor='black')
                            h(x) = 0.5*sqrt(2)*x
                            h1(x) = -1*h(x)
                            j(x) = 0.5*(f(4)-f(2))*(x-4)+2
                            j1(x) = -1*j(x)
                            shade1 = plot(h,(x,0,2),thickness=1, fill=h1,linestyle='dashed',color='black',fillalpha=0.25)
                            shade2 = plot(h1,(x,0,2),thickness=1,linestyle='dashed',color='black')
                            shade3 = plot(j,(x,2,4),thickness=1,fill=j1,linestyle='dashed',color='black',fillalpha=0.25)
                            shade4 = plot(j1,(x,2,4),thickness=1,linestyle='dashed',color='black',fillalpha=0.25)
                            geometry += p1
                            geometry += p2
                            geometry += f0
                            geometry += f2
                            geometry += f4
                            geometry += shade1
                            geometry += shade2
                            geometry += shade3
                            geometry += shade4
                            geometry
                        </sageplot>
                    </image>
            </figure>
                What is the  surface area of the frustum formed by rotating the line segment from <m>(2,\sqrt{2})</m> to <m>(4, 2)</m> about the <m>x</m>-axis?
                
                <ol marker='A'>
                    <li><m>2\pi \frac{4+\sqrt{2}}{2}\cdot\sqrt{2}</m></li>
                    <li><m>2\pi \frac{4+\sqrt{2}}{2}\cdot\sqrt{6}</m></li>
                    <li><m>2\pi \frac{4+\sqrt{2}}{2}\cdot\sqrt{6-2\sqrt{2}}</m></li>
                </ol>
            </p>
        </task>
        
        
        <task>
            <p>
                Suppose we wanted to estimate the  surface area with four frustums with <m>\Delta x=1</m>.
                
                <figure>
                <caption>Plot of bounded region rotated about <m>x</m>-axis.</caption>
                    <image xml:id="rotatingrootxaboutxfourpiece" width="50%">
                        <description>Bounded region rotated about <m>x</m>-axis.</description>
                        <sageplot>
                        from sage.plot.graphics import Graphics
                        geometry=Graphics()
                        x = var('x')
                        f(x) = sqrt(x)
                        g(x) = -1*f(x)
                        p1 = plot(f,(x,0,4),thickness=2,ticks=[1,0.5])
                        p2 = plot(g,(x,0,4),thickness=2,linestyle='dashed')
                        f0 = circle((0,0),0.05,fill=True,facecolor='black')
                        f1 = circle((1,f(1)),0.05,fill=True,facecolor='black')
                        f2 = circle((2,f(2)),0.05,fill=True,facecolor='black')
                        f3 = circle((3,f(3)),0.05,fill=True,facecolor='black')
                        f4 = circle((4,f(4)),0.05,fill=True,facecolor='black')
                        h0(x) = x #first line segment
                        h01(x) = -1*h0(x)
                        h1(x) = (f(2)-f(1))*(x-1)+1 #second line segment
                        h11(x) = -1*h1(x)
                        h2(x) = (f(3)-f(2))*(x-2)+f(2) #third line segment
                        h21(x) = -1*h2(x)
                        h3(x) = (f(4)-f(3))*(x-4)+2 #fourth line segment
                        h31(x) = -1*h3(x)
                        shade0 = plot(h0,(x,0,1),thickness=1,fill=h01,linestyle='dashed',color='black', fillalpha=0.25)
                        shade1 = plot(h1,(x,1,2),thickness=1,fill=h11,linestyle='dashed',color='black', fillalpha=0.25)
                        shade2 = plot(h2,(x,2,3),thickness=1,fill=h21,linestyle='dashed',color='black', fillalpha=0.25)
                        shade3 = plot(h3,(x,3,4),thickness=1,fill=h31,linestyle='dashed',color='black', fillalpha=0.25)
                        geometry += p1
                        geometry += p2
                        geometry += f0
                        geometry += f1
                        geometry += f2
                        geometry += f3
                        geometry += f4
                        geometry += shade0
                        geometry += shade1
                        geometry += shade2
                        geometry += shade3
                        geometry
                        </sageplot>
                    </image>
            </figure>
                
                
                <me>
                        \begin{array}{|c|c|c|c|c|c|}
                        \hline
                        x_i &amp; \Delta x &amp; r_i &amp; R_i &amp; l &amp; \text{Estimated Surface Area}\\
                        \hline
                        x_1=0 &amp; 1 &amp; 0 &amp; 1 &amp; \sqrt{1^2+1^2} &amp;\\
                        \hline
                        x_2=1 &amp; 1&amp; 1 &amp; \sqrt{2} &amp; \sqrt{1^2+(\sqrt{2}-1)^2} &amp; \\
                        \hline
                        x_3=2 &amp; 1&amp;  \sqrt{2} &amp; \sqrt{3} &amp; \\
                        \hline
                        x_4=3 &amp; 1 &amp; 3 &amp; 2 &amp; \\
                        \hline
                        
                        \end{array}
                    </me>
                
                
            </p>
        </task>
        
        
        <task>
            <p>
                Suppose we wanted to estimate the  surface area with <m>n</m> frustums.
                
                <figure>
                <caption>Plot of bounded region rotated about <m>x</m>-axis.</caption>
                    <image xml:id="rotatingrootxaboutxnpiece" width="50%">
                        <description>Bounded region rotated about <m>x</m>-axis.</description>
                        <sageplot>
                            from sage.plot.graphics import Graphics
                            geometry=Graphics()
                            x = var('x')
                            f(x) = sqrt(x)
                            g(x) = -1*f(x)
                            p1 = plot(f,(x,0,4),thickness=2,ticks=[1,0.5])
                            p2 = plot(g,(x,0,4),thickness=2,linestyle='dashed')
                            h(x) = 2*(f(2)-f(1.5))*(x-2)+f(2)
                            h1(x) = -1*h(x)
                            shade_line = plot(h,(x,1.5,2),linestyle='dashed',fill=h1,fillalpha=0.25,color='red')
                            x_leg = line(([1.5,h(1.5)],[2,h(1.5)]),linestyle='dashed',color='red')
                            y_leg = line(([2,h(1.5)],[2,h(2)]),linestyle='dashed',color='red')
                            x_leg_label = text('$\\Delta x$',(1.75,h(1.5)-0.15),color='red',fontsize=13)
                            c1 = circle((1.5,f(1.5)),0.05,facecolor='red',fill=True)
                            c2 = circle((2,f(2)),0.05,facecolor='red',fill=True)
                            geometry += p1
                            geometry += p2
                            geometry += shade_line
                            geometry += x_leg
                            geometry += y_leg
                            geometry += x_leg_label
                            geometry += c1
                            geometry += c2
                            geometry
                        </sageplot>
                    </image>
            </figure>
                
                
                Let <m>f(x)=\sqrt{x}</m>.  Which of  the following expressions represents the   surface area generated bo rotating the line segment from <m>(x_0, f(x_0))</m> to <m>(\Delta x, f(x_0+\Delta x))</m> about the <m>x</m>-axis?
                <ol marker='A'>
                    <li><m>\displaystyle \pi \left(\frac{f(x_0)+f(x_0+\Delta x)}{2}\right)^2\sqrt{(\Delta x)^2+(f(x_0+\Delta x)-f(x_0))^2}</m>.</li>
                    <li><m>\displaystyle 2\pi\frac{f(x_0)+f(x_0+\Delta x)}{2}\sqrt{(\Delta x)^2+(f(x_0+\Delta x)-f(x_0))^2}</m>.</li>
                    <li><m>\displaystyle 2\pi\frac{f(x_0)+f(x_0+\Delta x)}{2}\Delta x</m>. </li>
                </ol>
                
                
                
            </p>
        </task>
        
        
        <task>
            <p>
                 Which of  the following Riemann sums best estimates the  surface area of the solid generated by rotating <m>y=\sqrt{x}</m> over <m>[0,4]</m> about the <m>x</m>-axis ?  Let <m>f(x)=\sqrt{x}</m>.
                <ol marker='A'>
                    <li><m>\displaystyle \sum \pi \left(\frac{f(x_i)+f(x_i+\Delta x)}{2}\right)^2\sqrt{(\Delta x)^2+(f(x_i+\Delta x)-f(x_i))^2}</m>.</li>
                    <li><m>\displaystyle \sum 2\pi\frac{f(x_i)+f(x_i+\Delta x)}{2}\sqrt{(\Delta x)^2+(f(x_i+\Delta x)-f(x_i))^2}</m>.</li>
                    <li><m>\displaystyle \sum 2\pi\frac{f(x_i)+f(x_0+\Delta x)}{2}\Delta x</m>. </li>
                </ol>
            </p>
        </task>
        
        
    
    </activity>
    
    
    <fact>
        <statement>
            <p>
                Recall from <xref ref="fact-arclength"/> that
                <md>
                    <mrow>\lim_{\Delta x\to 0}\sqrt{(\Delta x)^2+(f(x_i+\Delta x)-f(x_i))^2} &amp; = \lim_{\Delta x\to 0} \sqrt{(\Delta x)^2\left(1+\left(\frac{f(x_i+\Delta x)-f(x_i)}{\Delta x} \right)^2\right)}</mrow>
                    <mrow>&amp;= \lim_{\Delta x\to 0} \sqrt{1+\left(\frac{f(x_i+\Delta x)-f(x_i)}{\Delta x} \right)^2}\Delta x</mrow>
                    <mrow>&amp;=\sqrt{1+(f'(x))^2}dx,</mrow>
                </md>
                and that 
                <me>
                    \lim_{\Delta x\to 0} \frac{f(x_i)+f(x_i+\Delta x)}{2}=f(x_i).
                </me>
            </p>
            
            <p>
                Thus given a function <m>f(x)\geq 0</m> over <m>[a,b]</m>, the  surface area of the solid generated by rotating this function about the <m>x</m>-axis is <me>SA=\int_a^b 2\pi f(x)\sqrt{1+(f'(x))^2}dx.</me>
            </p>
        </statement>
    </fact>
    
    <activity>
        <introduction>
            <p>
                Consider again the solid generated by rotating <m>y=\sqrt{x}</m> over <m>[0,4]</m> about the <m>x</m>-axis.
            </p>
        </introduction>
        <task>
            <p>
                Find an integral which computes the surface area of this solid.
            </p>
        </task>
        <task>
            <p>
                If we instead rotate <m>y=\sqrt{x}</m> over <m>[0,4]</m> about the <m>y</m>-axis, what is an integral which computes the surface area for this solid?
            </p>
        </task>
    </activity>
    
    
    <activity>
        <introduction>
            <p>
                Consider again the function <m>f(x)=\ln(x)+1</m> over <m>[1,5]</m>.
            </p>
        </introduction>
        <task>
            <p>
                Find an integral which computes the surface area of the solid generated by rotating the above curve about the <m>x</m>-axis.
            </p>
        </task>
        <task>
            <p>
                Find an integral which computes the surface area of the solid generated by rotating the above curve about the <m>y</m>-axis.
            </p>
        </task>
    </activity>
<!--     
    <subsection>
        <title>Slideshow</title>
        <p>Slideshow of activities available at <url href="AI4.slides.html"/>.</p>
    </subsection> -->
    </subsection>
    <subsection>
        <title>Videos</title>
    <figure>
		<video xml:id="video-AI4" youtube="nUFCAnuFTGc">
			<!--<track kind="subtitles" label="US English" xml:lang="en" source="videos/E1-captions.vtt" default=""/>-->
		</video>
        <caption>Video: Compute surface areas of surfaces of revolution</caption>
    </figure>
         
        
        
    </subsection>
    
    <subsection>
        <title>Exercises</title>
        <p>
            Exercises available at <url href="https://tbil.org/preview/calculus/exercises/#/bank/AI4/"/>
        </p>
    </subsection>
       
    
</section>

